// This route previously proxied isochrone requests to the Targomo API.
// It has been intentionally left empty because the isochrone feature is no
// longer in use. The file is kept only to avoid 404s if any stale clients
// still call `/api/isochrone`.
